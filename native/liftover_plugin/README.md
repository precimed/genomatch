Placeholder for the future packaged `bcftools +liftover` plugin source and build files.

The current migration only creates the target directory so native build work can land
without another repository reshuffle.
