This directory is reserved for future packaged runtime resources.

For the initial migration, `config.yaml` remains adjacent to the Python modules in
`src/genomatch/` so the current runtime lookup rules continue to work unchanged.
